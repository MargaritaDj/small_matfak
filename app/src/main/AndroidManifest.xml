<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mm"
        tools:targetApi="31">
        <activity
            android:name=".ui.screens.main.view.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>

        <activity android:name=".ui.screens.forgotpassword.view.ForgotPasswordActivity"
            android:label="Preferences">
            </activity>

        <activity android:name=".ui.screens.restorecode.view.RestoreCodeActivity"
            android:label="Preferences">
        </activity>

        <activity android:name=".ui.screens.newpassword.view.NewPasswordActivity"
            android:label="Preferences">
        </activity>

        <activity android:name=".ui.screens.area.student.view.PersonalAreaStudentActivity"
            android:label="Preferences">
        </activity>

        <activity android:name=".ui.screens.area.teacher.view.PersonalAreaTeacherActivity"
            android:label="Preferences">
        </activity>

        <activity android:name=".ui.screens.listtasks.student.view.ListTasksStudentActivity"
            android:label="Preferences">
        </activity>

        <activity android:name=".ui.screens.abouttask.view.AboutTaskStudentActivity"
            android:label="Preferences">
        </activity>

        <activity android:name=".ui.screens.solve.view.SolveTaskStudentActivity"
            android:label="Preferences">
        </activity>

    </application>

</manifest>